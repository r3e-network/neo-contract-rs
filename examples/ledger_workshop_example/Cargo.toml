[package]
name = "escrow-contract"
version = "0.1.0"
authors = ["Neo Contract Rust Team"]
edition = "2021"
description = "Escrow smart contract example demonstrating Ledger API usage"
readme = "README.md"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
neo-contract = { path = "../../crates/neo-contract" }

[dev-dependencies]
neo-contract-testing = { path = "../../crates/neo-contract-testing" }

[profile.release]
opt-level = 3
debug = false
lto = true
codegen-units = 1
panic = "abort"
overflow-checks = true

[profile.test]
opt-level = 0
debug = true
overflow-checks = true

[package.metadata.neo.manifest]
name = "EscrowContract"
groups = []
abi = { methods = [], events = [] }
permissions = [
    { contract = "*", methods = "*" },
]
trusts = []
extra = {
    author = "Neo Contract Rust Team",
    email = "dev@neo.org",
    description = "Escrow smart contract example demonstrating Ledger API usage"
} 