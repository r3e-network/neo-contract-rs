[package]
name = "ink_style_token"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]


[dependencies]
neo-contract = { path = "../../neo-contract" }
neo-macros = { path = "../../neo-macros" }
wee_alloc = "0.4.5"
