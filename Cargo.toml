[workspace]
members = [
    "neo-contract",
    "neo-compiler",
    "neo-macros",
    "neo-contract-testing",
    "examples/dao",
    "examples/hello_world",
    "examples/hello_with_macros",
    "examples/token_with_annotations",
    "examples/annotation_test",
]

resolver = "2"

[profile.release]
lto = true
opt-level = 's' # Optimize for size
codegen-units = 1
panic = 'abort'

# Special configuration for WASM compilation
[profile.release.package.neo-contract]
opt-level = 'z' # Optimize aggressively for size
