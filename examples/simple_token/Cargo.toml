[package]
name = "simple_token"
version = "0.1.0"
edition = "2021"

[dependencies]
neo-contract = { path = "../../neo-contract" }
wee_alloc = "0.4.5"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
debug = false
lto = true
codegen-units = 1
panic = "abort"