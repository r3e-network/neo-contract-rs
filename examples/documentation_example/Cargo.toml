[package]
name = "documentation_example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
neo-contract = { path = "../../neo-contract" }

[features]
# This feature is used when building in release mode
# It enables wasm-opt and other optimizations
release-build = []