[package]
name = "neo-meme-coin"
version = "0.1.0"
edition = "2021"

[dependencies]
neo-contract = { path = "../../neo-contract" }
neo-macros = { path = "../../neo-macros" }

[lib]
crate-type = ["cdylib"]